# devsecops-pipeline

Drop-in DevSecOps pipeline for GitHub Actions — secrets, SAST, dependencies, IaC, containers, and SBOM in one reusable workflow.

> This README is populated in the final step of the initial build. See `CLAUDE.md` for the spec and `CHANGELOG.md` for what's shipped.
