# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp
*.swo
*~

# Local secrets — public-safe repo, should never appear, but belt-and-braces
.env
.env.*
!.env.example

# Local scanner output (when running scanners outside CI)
*.sarif
scan-summary.json
sbom-*.json
sbom-*.xml

# Node (in case `pr-comment.js` is ever exercised locally)
node_modules/

# Python venvs (in case docs tooling is ever added)
.venv/
__pycache__/

# act (local Actions runner) artifacts
.act/
